#include<iostream>
using namespace std;
int main() {
    int a = 2;      //output is 0 and if you put a = 0 then output is 1
    cout << !a << endl;     //!(not operator) prints just opposite if 
    return 0;
} 
